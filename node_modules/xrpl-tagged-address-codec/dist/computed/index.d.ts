import * as types from '../types';
declare const IsTestnet: (destination: string | types.Destination) => boolean;
declare const CodecOptions: (destination: string | types.Destination) => types.CodecOptions;
export { CodecOptions, IsTestnet };
