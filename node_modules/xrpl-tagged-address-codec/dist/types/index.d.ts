declare type Destination = {
    account: string;
    tag?: null | number | string;
    testnet?: boolean;
};
declare type CodecOptions = {
    version: number | Uint32Array;
    expectedLength: number;
};
export { Destination, CodecOptions };
